/*
 * a really simple example that removes an element from the page.
 * in this case, just an H1 element.
 * 
 * using the jquery short hand
 * this is probably the most common format that you'll be using.
 * 
 * */

// jquery dom ready function.
$(function(){
	$("h1").remove();
});